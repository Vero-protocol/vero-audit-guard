[package]
name = "audit-guard"
version = "0.1.0"
edition = "2021"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
attohttpc = { version = "0.31", default-features = false, features = ["json"] }
thiserror = "1.0"
